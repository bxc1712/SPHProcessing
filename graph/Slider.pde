class Slider {
  boolean overHandle = false;
  float sliderWidth;
  float handleHeight;
  //Slider Container
  float containerWidth;
  float screenWidth;
  float screenHeight;
  
  Slider(float width,float height) {
    screenWidth=width;
    screenHeight=height;
  }
  
  void draw(){
  }
  
  void mousePressed(){
    if(overHandle){
      
    }
  }
}